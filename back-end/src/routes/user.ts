import { Router, Request, Response } from "express";

const router = Router();

//                        here is the controller place
router.post("/save", function (req: Request, res: Response) {

});

export default router;